# ISSUES — BUG / GAP Tracker

**Canonical home for every tracked defect, capability gap, and recurrence in Elefante.** Each row links to its postmortem in [`postmortems/<domain>.md`](postmortems/) and to its verification command. Lifecycle step 1 (CLASSIFY) reads this file to match work against existing rows.

> **Cross-refs:** Domain postmortems = [`postmortems/`](postmortems/). Cross-bug rules = [`lessons.md`](lessons.md). Active release state + journal = [`PLANNING.md`](PLANNING.md). Constitution = [`../agents/orchestrator.md`](../agents/orchestrator.md).
>
> **Last Updated:** 2026-09-03 (live graph relationship display repair; 68 distinct BUG records through BUG-070 + 9 GAPs)

---

## MANDATORY: Read This First

**Every debugging session starts here.** Check Known Issues below. If the error matches an open or recurring issue, follow the compendium link and run the verification command. Do not skip to source code.

```
Entry flow:  README.md (this file) → Known Issues → Compendium → Verification Commands → Test
Exit flow:   Fix → Test passes → Update compendium → Close issue here → docs/how-to/close-a-feature.md closure
```

---

## Known Issues & Development Priorities

Active bugs and recurring failure classes. Each links to its compendium post-mortem and test gate.

| ID | Issue | Status | Compendium | Verification Command | Recurrence |
| -- | ----- | ------ | ---------- | -------------------- | ---------- |
| BUG-045 | Release-candidate tooling and workflows embedded v2.12.2 in candidate metadata, archive paths, and the installed-version assertion; the first repair then imported dependency-backed `src` before runner setup. Both candidate CI lanes rejected valid v2.12.3 artifacts. | FIXED (guarded) — standard-library tooling derives candidate identity and exposes dependency-free version discovery; workflows carry that value through paths and installation assertions | [installation #24](postmortems/installation.md#issue-24) | `pytest tests/test_release_client_bundle.py tests/test_release_pipeline.py -q`; run the Quality dashboard and Build Release Client Candidate jobs | 1x — v2.12.3 publication gate 2026-08-25 |
| BUG-044 | Active release, scoring, ETL, Distiller, dashboard, showcase, and proposal surfaces described stale or unimplemented behavior; existing guards themselves pinned obsolete product state. | FIXED AGAIN (guarded), unreleased — Home states one product purpose and three operator jobs. Review priority lives in Memory Intelligence, explicit trails in Connections, and any Recall Briefing requires a real event; no heuristic inventory memory is presented as task evidence. Recall has its own bounded project-scoped Inspector; Memory Intelligence separates Library from evidence-only Review; Connections remains first-class. The source shell defaults to clear light and preserves dark. The HTML guide and PRD match the source/install boundary. Website synchronization and customer-package acceptance remain separate gates. | [ai behavior #13](postmortems/ai-behavior.md#issue-13) | `./.venv/bin/python -m pytest tests/test_dashboard_ui.py tests/test_dashboard_serializer.py -q`; `npm run build` in `src/dashboard/ui`; isolated showcase browser acceptance across all six workspaces and both themes | 6x — post-release whole-product audit 2026-08-25; Gauntlet stale-surface recurrence 2026-08-28; four-action index recurrence 2026-08-30; live HTML-guide recurrence 2026-08-31; dashboard inference recurrence 2026-08-31; source prototype repair and evidence-hierarchy recurrence 2026-09-01 |
| BUG-028 | Dashboard served private memory data with wildcard CORS and a public bind default; Compose also published port 8000 on all interfaces. | FIXED (guarded) — loopback bind, explicit origins, loopback Compose publication | [dashboard #10](postmortems/dashboard.md#issue-10) | `pytest tests/test_dashboard_serializer.py -k "loopback or cors" -v` | 0x — audit discovery 2026-07-22 |
| BUG-029 | `elefante-GraphQuery` was described as retrieval but permitted graph writes through Cypher, bypassing the explicit GraphConnect path. | FIXED (guarded) — validator enforced at the MCP boundary | [database #9](postmortems/database.md#issue-9) | `pytest tests/test_dashboard_serializer.py -k "graph_query_validator" -v` | 0x — audit discovery 2026-07-22 |
| BUG-030 | Default `pytest` invocation collected manual tests and failed on duplicate module names, so the automated suite could not be used as a release gate. | FIXED (guarded) — documented `pytest tests` command collects only automated coverage | [ai behavior](postmortems/ai-behavior.md) | `pytest tests -q` | 0x — audit discovery 2026-07-22 |
| BUG-031 | Dashboard claimed to be a static read-only inspection surface but opened live ChromaDB for graph hydration and semantic search; its browser API could also spawn a snapshot-generation subprocess. | FIXED (guarded) — every dashboard data endpoint reads the redacted snapshot only; the browser can reload but cannot regenerate it. Live refresh remains the explicit MCP/CLI path. | [dashboard #11](postmortems/dashboard.md#issue-11) | `pytest tests/test_dashboard_serializer.py -k "snapshot or dashboard" -v`; `npm run build` in `src/dashboard/ui` | 0x — audit discovery 2026-07-22 |
| BUG-032 | A direct VS Code adapter test could omit `manifest_home`, causing temporary pytest paths to be recorded in the user's real `~/.elefante/install-manifest.json`. | FIXED (guarded) — the low-level adapter requires an explicit manifest home; the production caller passes `Path.home()` and tests pass isolated homes. Residual test-only entries are quarantined before live migration. | [installation #15](postmortems/installation.md#issue-15) | `pytest tests/test_install_setup.py -k "manifest_home or transport_only_bridge" -v` | 1x — residual state discovered before live migration 2026-07-23 |
| BUG-033 | Orphaned/stale background server process listening on port 8000 from a trashed repository directory (/Users/jay/.Trash/elefante) served broken responses (HTTP 500) to dashboard requests. | FIXED (guarded) — killed stale process, verified CWD and clean snapshot responses | [dashboard #12](postmortems/dashboard.md#issue-12) | `pytest tests/test_dashboard_serializer.py -k "null_name or graph" -v` | 1x — stale process from trashed directory 2026-07-28 |
| BUG-034 | The v2.12 dashboard header displayed a clipped left fragment instead of the complete canonical elephant-and-network emblem. | FIXED (guarded) — the asset is the complete canonical symbol crop, its dimensions and digest are locked, and the hover overlay is restricted to the network region. | [dashboard #13](postmortems/dashboard.md#issue-13) | `pytest tests/test_dashboard_serializer.py -k "brand_assets" -v`; `npm run build` in `src/dashboard/ui`; inspect the live header | 0x — caught by user during pre-merge visual acceptance 2026-07-26 |
| BUG-035 | The PR lock-freshness job compiled in an empty directory, so newly published transitive dependencies made an unchanged valid lock appear stale. | FIXED (guarded) — CI seeds the checked-in lock before recompilation, preserving valid transitive pins while still detecting requirement-driven changes. | [installation #16](postmortems/installation.md#issue-16) | `pytest tests/test_release_pipeline.py -k "lock_freshness" -v`; reproduce the workflow compile in a clean temporary directory containing both dependency files | 0x — PR #7 Quality failure 2026-07-26 |
| BUG-036 | A persistence test required `~/.elefante/data/chroma` to exist even though SQLite is the default. It passed locally only because a historical Chroma directory remained on disk. | FIXED (guarded) — the test compares active store paths with the data root from the same configuration instance, distinguishes the configured vector directory from retired Chroma state and the Kuzu-owned lazy path, then exercises the default contract under a genuinely fresh home. | [database #10](postmortems/database.md#issue-10) | `pytest tests/test_memory_persistence.py -k "config_paths_exist or fresh_home" -v`; run with an isolated `HOME` | 0x — replacement PR #7 Quality run 2026-07-26 |
| BUG-037 | Published installer ZIPs exposed generic cross-platform script bundles instead of an obvious platform entrypoint; the v2.11.1 Windows launcher also encoded `\b` as a hidden backspace byte inside `bootstrap_release_bundle.py`, breaking its launch path. | FIXED (guarded), published asset repair 2026-07-29 — v2.11.1 assets now expose only their platform launchers; local generation adds stable Unix execute metadata, CRLF Windows output, and rejects hidden control bytes. | [installation #17](postmortems/installation.md#issue-17) | `pytest tests/test_installer_bundle.py -v`; re-download all three GitHub assets; compare SHA-256; inspect root entries and launcher bytes | 1x — stakeholder download audit 2026-07-29 |
| BUG-038 | `bootstrap_release_bundle.py --dry-run` placed the payload and moved an existing installation before checking the dry-run flag. | FIXED (guarded), published asset repair 2026-07-29 — dry-run builds and prints the delegated command before payload placement; a regression fails if `place_payload` is called or the target path appears. | [installation #18](postmortems/installation.md#issue-18) | `pytest tests/test_installer_bundle.py -k "dry_run" -v`; run a downloaded launcher with `--install-root <absent-temp-path> --dry-run`; assert the path remains absent | 1x — discovered during BUG-037 destination validation 2026-07-29 |
| BUG-039 | Fresh-install configuration selected SQLite, but the database initializer still described and reported Chroma-specific storage paths. | FIXED (guarded) — initializer messages and verification derive both storage paths from the active configuration. | [installation #19](postmortems/installation.md#issue-19) | `pytest tests/test_install_setup.py -k "configured_storage_paths" -v` | 0x — v2.12 release audit 2026-07-30 |
| BUG-040 | A customer installation or repair could remain checkout-bound, retain an unowned older Elefante host entry, or verify a different MCP path than customers use. | FIXED (guarded), released v2.12.2 — one stable runtime owns every detected compatible host; customer scope adopts only structurally identified older Elefante registrations; install verification exercises the real stdio bridge and daemon. | [installation #20](postmortems/installation.md#issue-20) | `./.venv/bin/python -m pytest tests/test_installer_bundle.py tests/test_install_setup.py tests/test_installer_gui.py tests/test_mcp_handshake_verifier.py -q` | 2x — first-customer journey plus live v2.12.2 repair 2026-08-05 |
| BUG-041 | A customer download was a repository-like snapshot containing developer workspace, tests, migration/developer utilities, and the development dependency lock. | FIXED (guarded), released v2.12.2 — the customer builder uses a strict runtime allowlist and separate hash-locked runtime dependencies. | [installation #21](postmortems/installation.md#issue-21) | `./.venv/bin/python -m pytest tests/test_release_client_bundle.py tests/test_installer_bundle.py tests/test_install_setup.py -q`; build and independently verify the client archive | 1x — first-customer macOS install audit 2026-08-04 |
| BUG-042 | A genuine clean-machine install crashed before creating its virtual environment because `install.py` imported `src`, which required `pydantic` before dependencies were installed. | FIXED (guarded), released v2.12.2 — installer startup reads the version declaration without importing dependency-backed product modules; a `python -S` regression proves the pre-dependency entrypoint. | [installation #22](postmortems/installation.md#issue-22) | `./.venv/bin/python -m pytest tests/test_install_setup.py -k "entrypoint_starts_without_product_dependencies" -v`; real fresh macOS client workflow | 1x — isolated macOS release-candidate install 2026-08-05 |
| BUG-043 | A clean client installation completed runtime setup but failed final health verification because the check required developer-only SDD directives and specification memories. | FIXED (guarded), released v2.12.2 — health verification selects the client or developer baseline from the runtime profile and rejects developer-directive leakage into a client runtime. | [installation #23](postmortems/installation.md#issue-23) | `./.venv/bin/python -m pytest tests/test_install_setup.py -k "client_health_checks_customer_baseline" -v`; real fresh macOS client workflow | 1x — isolated macOS release-candidate install 2026-08-05 |
| BUG-046 | The Task Intelligence benchmark could reject behaviorally correct repairs because hidden acceptance tests required undisclosed historical modules, symbols, source strings, messages, or thresholds. Retrieval and lower cost could therefore appear ineffective under an invalid judge. | EVALUATOR FIXED (guarded), effectiveness OPEN — self-test proves nine canaries base-fail/known-fix-pass; new trials record judge→acceptance stage traces and invalid judges are blocked by default. New controlled tasks showed lower cost but no repeatable correctness lift; 23 tasks remain ineligible and product promotion remains blocked. | [ai behavior #14](postmortems/ai-behavior.md#issue-14) | `python scripts/ci/verify_task_intelligence_benchmark.py --verify-canaries`; `--require-promotion-ready` must exit 2; focused evaluator tests | 2x — judge audit plus black-box causal canaries 2026-08-06 |
| BUG-047 | Recall confused shared topics with requested facts, missed useful paraphrases, and let repeated natural-language words veto independently qualified task guidance. | FIXED LOCALLY (guarded), candidate; not publicly released — preserve the shared bundle and existing model/thresholds. Repeated anchors cannot reject qualified role evidence; explicit properties and quantities require supporting evidence even without saved cues. 116 focused Recall tests pass, including ordinary uncued facts and the real price/month negative. Broader language accuracy is not claimed. | [ai behavior #15](postmortems/ai-behavior.md#issue-15) | `pytest tests/test_task_intelligence.py tests/test_proactive_surfacing.py tests/test_mcp_daemon.py -q`; real development-question browser Recall plus missing-fact abstention | 7x — ordinary-question/Keep both and natural-task recurrences; focused repair 2026-09-03 |
| BUG-048 | Normal search and automatic context delivery treated retrieval exposure as memory use: access counts and co-activation could change future ranking before a caller confirmed that the memory informed a task. This contaminated lifecycle signals and Task Intelligence evaluation. | FIXED (guarded), included in v2.13.0 — retrieval is read-only; legacy exposure history is discarded; trace-bound `record_use` writes a reversible observational event only and cannot mutate ranking. | [ai behavior #16](postmortems/ai-behavior.md#issue-16) | `pytest tests/test_autonomous_coactivation.py -k "exposure or coactivation" -v`; `pytest tests/test_task_intelligence_ledger.py -q` | 0x — root-cause audit 2026-08-08 |
| BUG-049 | Governance fields existed without an enforced authority boundary: workflow automation could claim permanent/user-locked policy, protected memories could be changed by automation or archived by refinery cleanup, and delete meant immediate permanent removal. | FIXED (guarded), included in v2.13.0 — invocation authority is explicit; workflow calls cannot create or weaken protected policy; protected memories survive refinery cleanup; delete archives by default and permanent removal requires user-directed confirmation. | [memory #16](postmortems/memory.md#issue-16) | `pytest tests/test_mcp_daemon.py -k "authority or protected or delete" -v`; `pytest tests/test_refinery.py -k "protected" -v` | 0x — governance implementation audit 2026-08-08 |
| BUG-050 | Task Intelligence had a compiler and benchmark but no safe production lifecycle tying one host invocation to retrieval, delivery, declared use, outcome, inspection, reversal, and a real pre-existing durable-memory golden path. | INFRASTRUCTURE FIXED (guarded), effectiveness OPEN — the default-off tool, session-bound metadata-only ledger, sealed fixtures, deterministic preflight, strict source-only versus memory-component comparison, and decision-complete early stop close the evaluation loop. Task 032 selected/delivered memory 3/3 but accepted 0/3, so release promotion remains blocked. | [ai behavior #17](postmortems/ai-behavior.md#issue-17) | `pytest tests/test_task_intelligence_ledger.py tests/test_task_intelligence_evaluation.py tests/test_task_intelligence_report.py -q`; task 032 preflight and `--require-decision` report | 0x — production-loop audit 2026-08-08; causal component STOP 2026-08-13 |
| BUG-051 | Task Intelligence evaluation could lose the failed workspace, reuse a stale outcome after the task/judge changed, starve later evidence stages, select repeated or wrongly classified sources, and let a hidden environment convention turn a correct behavioral patch into a false failure. | FIXED (guarded), effectiveness OPEN — failed workspaces are preserved by default; schema-v3 paths bind the complete task contract; the compiler reserves governed stage/item budgets and source diversity; tests are safeguards; declared context chunks reach semantic selection; canaries disclose observable conventions and remain base-fail/known-fix-pass. One preserved real-memory treatment passes the corrected judge, but paired multi-task lift remains unproven. | [ai behavior #18](postmortems/ai-behavior.md#issue-18) | `pytest tests/test_task_intelligence.py tests/test_task_intelligence_evaluation.py tests/test_task_intelligence_report.py -q`; `python scripts/ci/verify_task_intelligence_benchmark.py --verify-canaries`; preflight task 031 | 1x — treatment diagnosis and judge replay 2026-08-09 |
| BUG-052 | Normal search, the context prompt, and opt-in tool-response delivery reused the governed selector but skipped its current-source annotation step. A digest-stale user-locked memory could therefore be delivered on those paths while the explicit Task Brief correctly blocked it. | FIXED (guarded), included in v2.13.0 — every runtime answer-delivery path now deep-copies and source-checks candidates through one shared boundary before compiling context; broad search remains inspectable. | [ai behavior #19](postmortems/ai-behavior.md#issue-19) | `pytest tests/test_mcp_daemon.py -k "digest_stale" -q`; `pytest tests/test_task_intelligence.py -k "current_source" -q` | 0x — current-tree production audit 2026-08-09 |
| BUG-053 | A global Elefante registration still did not make Codex retrieve memory for a normal question; an explicit Recall could request approval, a direct factual answer could be rejected by an implementation-actionability threshold, and successful output carried internal protocol noise. Later packaging/readiness drift could install Recall guidance ahead of the live tool, response serialization could multiply token cost, SDK-side input validation could replace the seven-field contract, and two packaged host allowlists omitted Recall and Directive tools. | FIXED AGAIN (guarded), published in v2.13.0 with unreleased input/host-metadata hardening — the real customer archive binds default-on read-only Recall to its routing contract; doctor verifies active guidance plus live annotations and bounded probe; registration/guidance setup rolls back atomically. Recall is called at most once, keeps the seven-field contract for every terminal path, does not echo the question, preserves Unicode, and fails closed above the 1,000-token complete-response budget. Antigravity and VS Code/Bob approval metadata now derive from one exact source-checked 17-tool customer inventory. | [ai behavior #20](postmortems/ai-behavior.md#issue-20) | `pytest tests/test_mcp_daemon.py -k "recall" -q`; `pytest tests/test_developer_routing.py -k "host_auto_allow" -q`; focused installer/archive/doctor suites; isolated self-protocol; clean external Codex normal-question event | 3x — Recall/readiness/token audit 2026-08-26; invalid-input and host-metadata recurrences 2026-08-28; installed-runtime replacement and live external Codex acceptance remain separate operator/certification actions |
| BUG-054 | An installed development runtime can execute code that differs from the published release while reporting the same semantic version; the install manifest records no source commit or release channel. A later PR candidate embedded GitHub's temporary merge SHA instead of the durable reviewed head SHA, and all three source-identity helpers later ignored untracked payload source. | FIXED AGAIN (guarded), included in v2.13.0 with unreleased identity hardening; installed runtime remains separate — customer payloads carry an internal build identity; archive verification, bootstrap, installer, schema-v3 ownership manifest, and `doctor` require the same version, clean source commit, and candidate/release channel. Candidate artifact workflows bind the durable source SHA; source identity includes tracked and untracked files; downloaded checksums use portable basenames. Development bundles declare `development`, and legacy, dirty, or mismatched customer identities fail readiness. | [installation #25](postmortems/installation.md#issue-25) | `pytest tests/test_installer_bundle.py tests/test_release_client_bundle.py tests/test_install_setup.py tests/test_release_pipeline.py -q`; exact candidate install must report `source_commit == SOURCE_COMMIT`; downloaded `shasum -a 256 -c SHA256SUMS` | 2x — version-integrity audit 2026-08-13; ephemeral PR-merge artifact caught 2026-08-14; untracked-source identity recurrence 2026-08-28 |
| BUG-055 | A long-lived stdio host kept the daemon-issued MCP session ID forever. After the local daemon restarted, every later tool call returned HTTP 404 until the host process itself reconnected, making installed Elefante appear unavailable even though the replacement daemon was healthy. | FIXED LOCALLY (guarded), unreleased — the storage-free bridge retains the successful client handshake and, only after a stale-session 404, performs one fresh initialization, replays the initialized notification, and retries the interrupted request once. Other HTTP failures are not retried. Unit proof covers request and notification recovery; a real bridge/daemon replacement proves the exact 17-tool customer inventory and a seven-field Recall result after recovery. | [installation #26](postmortems/installation.md#issue-26) | `pytest tests/test_mcp_daemon.py -k "stdio_bridge or bridge_reinitializes" -q`; `pytest -c tests/pytest.ini tests/test_mcp_daemon.py::test_bridge_reinitializes_against_a_restarted_daemon -m slow -q` | 1x — live Codex task lost its daemon session after runtime replacement 2026-08-28 |
| BUG-056 | Secret scrubbing covered normal memory writes but not every nested GraphConnect property or ETL response/enrichment path. Legacy raw memories could be returned with secrets, and agent-provided ETL metadata could persist them. | FIXED LOCALLY (guarded), unreleased — GraphConnect scrubs the complete request before persistence; ETLProcess scrubs the complete agent-facing result; ETLClassify scrubs summary, concepts, and trigger metadata before persistence. Each response reports only redaction counts and types. | [ai behavior #25](postmortems/ai-behavior.md#issue-25) | `pytest tests/test_mcp_daemon.py -k "graph_connect_scrubs or etl_process_scrubs or etl_classify_scrubs" -q` | 0x — Gauntlet privacy-boundary audit 2026-08-28 |
| BUG-057 | GraphConnect and ETLProcess acquired the transaction-scoped write lock only as an entry check, released it, and then performed graph or queue-state mutations outside the protected lifecycle. | FIXED LOCALLY (guarded), unreleased — GraphConnect holds the lock through every entity and relationship write; ETLProcess holds it while raw rows are marked processing and while related stats are read. Optional read-only status rendering happens after the graph lock is released. | [database #11](postmortems/database.md#issue-11) | `pytest tests/test_mcp_daemon.py -k "graph_connect_scrubs or etl_process_scrubs" -q` | 0x — Gauntlet transaction-scope audit 2026-08-28 |
| BUG-058 | The privileged Kuzu-only reset ignored active configuration, targeted the real default user path, and claimed it rebuilt graph state from Chroma even though it only moved one hard-coded path. Its first repair still trusted any configured external directory after one generic confirmation. | FIXED AGAIN LOCALLY (guarded), unreleased — dry-run and apply resolve the configured graph path without opening stores, preserve the vector path, move the graph into a timestamped recovery directory, reject root/home/storage-ancestor targets, require exact path confirmation for storage outside the data root, and state that no automatic rebuild occurs. The audit-triggered live move was restored immediately from its exact timestamped backup; installed doctor returned customer-ready. | [database #12](postmortems/database.md#issue-12) | `pytest tests/test_backup_restore.py -k "kuzu_only_reset" -q`; installed `doctor.py --json` after exact restoration | 2x — destructive-target regression exposed/restored, then external-path critic recurrence closed 2026-08-28 |
| BUG-059 | Conflict correction treated a successful adapter return as product completion, refreshed Home with a direct non-atomic write, and could misclassify a partial adapter mutation as no change. The authoritative store, Home snapshot, and scoped Recall could therefore disagree while the caller received a success-shaped result. | FIXED LOCALLY (guarded), unreleased — Verified Resolve now plans against exact record and scope hashes, performs one semantic write, reads both records back, atomically publishes one generated snapshot, verifies scoped Recall, and returns only terminal completion states. Failed postconditions trigger an exact two-record compensating restore; incomplete rollback is `UNSAFE`, never success. Home uses a short-lived origin-bound capability and a one-use plan ticket instead of exposing generic write access. | [memory #17](postmortems/memory.md#issue-17) | `pytest tests/test_verified_resolve.py tests/test_home_control.py tests/test_atomic_json.py tests/test_conflict_resolution.py tests/test_dashboard_serializer.py -q` | 0x — adversarial Memory Control Loop implementation audit 2026-08-29 |
| BUG-060 | Project identity existed only as optional memory metadata. A missing registry could therefore fall back to global compatibility, Home could fabricate compatibility from an absent snapshot, registry and dashboard projection writes could diverge, and a temporary configured data root still used the account-global write lock. | FIXED LOCALLY (guarded), unreleased — one private versioned Project Registry now owns stable IDs and deterministic deepest-root mapping; a separate mode-0600 strict-intent marker makes missing or invalid isolation state fail closed; Remember, Search, and Recall enforce one resolved project before store access; Home exposes only named project controls and publishes registry, intent, and snapshot with exact rollback; unavailable or invalid projection state remains unavailable; write locks follow the configured data installation. | [memory #18](postmortems/memory.md#issue-18) | `pytest tests/test_atomic_json.py tests/test_write_lock_isolation.py tests/test_project_registry.py tests/test_project_scoping.py tests/test_home_control.py tests/test_dashboard_serializer.py tests/test_dashboard_snapshot_verifier.py tests/test_install_setup.py tests/test_mcp_daemon.py -m "not integration and not slow" -q`; `npm run build` in `src/dashboard/ui` | 0x — project-safe Memory Control Loop implementation and rendered acceptance 2026-08-29 |
| BUG-061 | Customer correction was split across low-level update, resolve, and delete verbs with different authority and completion semantics. Content or lifecycle changes could bypass graph, Home, and Recall verification; Home exposed no coherent correction journey; permanent deletion had no proven recovery baseline; and the dashboard later hid every action for a superseded archived record even though verified permanent deletion remained applicable. | FIXED LOCALLY (guarded), unreleased — one verified correction boundary now owns Edit, Replace, Archive, Restore, and advanced permanent deletion while Resolve preserves explicit winner authority. Plans bind exact record, graph, relationship, content, and project hashes; verified writes reconcile deterministic concept links while preserving explicit relationships; every path proves authoritative SQLite/Kuzu, atomic Home, and scoped Recall or restores exact preimages. Permanent deletion requires a separate final confirmation and one exact live workflow backup, removes the selected memory, generated links, graph entity, safe orphan source, and unshared attachments, proves Home/Recall absence, then destroys the temporary backup; failure restores it. Manually archived records expose Restore and permanent deletion; other inactive records expose only the verified permanent-deletion plan. Legacy mutations fail closed into Correct, and Home uses content-free one-use tickets and privacy-safe receipts. | [memory #19](postmortems/memory.md#issue-19) | `pytest tests/test_verified_correction.py tests/test_verified_recovery.py tests/test_verified_resolve.py tests/test_home_control.py tests/test_mcp_daemon.py tests/test_dashboard_serializer.py tests/test_dashboard_ui.py -q`; `npm run build` in `src/dashboard/ui`; desktop and 390 x 844 localhost browser acceptance with synthetic data verifies the warning, exact `DELETE` gate, reachable controls, and `Recoverable: No` receipt; exact-package and keyboard/accessibility acceptance remain separate release gates | 1x — Complete Correct and recovery-bound erasure audit 2026-08-30; inactive-record dashboard route recurrence 2026-09-02 |
| BUG-062 | Home accepted the official package receipt but silently discarded its `safety_backup` and `product_readiness` checks because the Recover allowlist knew the package codes but not the two package check names. A real three-check package result could therefore render as only one check. | FIXED LOCALLY (guarded), unreleased — the privacy allowlist now recognizes the two fixed package check names, keeps arbitrary names and text excluded, accepts interrupted `RUNNING` receipts, and the regression generates the receipt through the real package writer before reading it through Recover. | [installation #27](postmortems/installation.md#issue-27) | `pytest tests/test_verified_recovery.py::test_health_exposes_only_allowlisted_package_handoff_receipt tests/test_verified_recovery.py::test_support_report_preview_and_export_are_strictly_allowlisted -q`; `ruff check src/core/verified_recovery.py tests/test_verified_recovery.py`; `git diff --check` | 0x — four-action product assembly audit 2026-08-30 |
| BUG-063 | Verified Remember compared the authoritative memory with a graph projection whose creation time was generated independently a few milliseconds later. Real SQLite/Kuzu writes therefore passed individually but could never share the exact graph hash expected by the verifier, so every genuine Remember rolled back while fake-store tests stayed green. | FIXED LOCALLY (guarded), unreleased — the orchestrator now gives the graph entity the memory's canonical creation time; the verifier and persisted graph hash agree, and a real SQLite/Kuzu regression prevents fake-store coverage from masking the integration boundary. | [memory #20](postmortems/memory.md#issue-20) | `pytest tests/test_verified_remember.py -q`; isolated four-action dogfood receipt must report `VERIFIED_COMPLETE` for Remember, Correct, and Recover plus scoped Recall `supplied` | 0x — Elefante-builds-Elefante dogfood audit 2026-08-30 |
| BUG-064 | Home's live snapshot hydration treated Kuzu entity rows as model objects and expected one exact relationship-label key. The current driver returns entity mappings plus a version-shaped label key, so real hydration logged an error and silently omitted Kuzu entities and labels while derived topic links made Connections appear healthy. | FIXED LOCALLY (guarded), unreleased — one shared serializer now normalizes supported Kuzu entity shapes, preserves relationship labels across driver result shapes, and rejects self-loop or dangling endpoints in both live and standalone snapshot producers. | [dashboard #14](postmortems/dashboard.md#issue-14) | `pytest tests/test_dashboard_serializer.py tests/test_verified_remember.py -q`; an isolated current-driver query must hydrate without the former mapping-attribute error | 0x — Elefante-builds-Elefante Home audit 2026-08-30 |
| BUG-065 | Home asked how the customer might ask for a new memory later, but discarded that answer after the immediate check. Exact STAC-style wording could therefore write successfully, fail semantic Recall, roll back, and surface a generic “related knowledge changed” message instead of the real outcome. | FIXED LOCALLY (guarded), unreleased — bounded `recall_cues` persist with the memory; exact complete-question matches use a separate strict project/workspace path that still enforces lifecycle, reliability, governance, current-source, conflict, and privacy gates. Remember, Edit, Replace, and Restore maintain those cues, while failed Remember reports that Recall could not be proved and nothing was saved. | [memory #21](postmortems/memory.md#issue-21) | `pytest tests/test_verified_remember.py tests/test_verified_correction.py tests/test_proactive_surfacing.py tests/test_dashboard_serializer.py tests/test_dashboard_ui.py -q`; isolated browser Remember/Recall with the exact STAC content and future question | 0x — exact customer STAC Remember reproduction 2026-08-30 |
| BUG-066 | Task Intelligence resolved strict project scope from the caller's raw workspace string. On macOS, `/var/...` and the registry's canonical `/private/var/...` name the same directory but compared unequal, so Task Intelligence abstained while Recall worked. | FIXED LOCALLY (guarded), unreleased — Task Intelligence now resolves the same registered-project boundary as Recall and searches with the canonical registered root; omission reasons remain explicit. | [ai behavior #26](postmortems/ai-behavior.md#issue-26) | `pytest tests/test_mcp_daemon.py tests/test_task_intelligence.py -k "task_intelligence and (project or workspace or canonical)" -q`; isolated self-protocol under a symlinked temporary root | 0x — strict-project self-protocol recurrence 2026-08-30 |
| BUG-067 | Home Remember classified explicit “must” versus “must not” assertions as duplicates; Keep both could not persist bilateral conflict identity, its Recall postcondition incorrectly required one unresolved side to be supplied instead of safely withholding both, and a later high similarity score paused unrelated same-project prose. | FIXED LOCALLY (guarded), unreleased — the conservative proposition parser handles explicit modal negation, contradiction takes precedence over near-duplicate similarity, and similarity-only `related` handling requires two shared substantive concepts after removing project-name and generic memory terms. Explicit conflict IDs create bilateral graph/store projections, and conflict Remember completes only when scoped Recall withholds both sides and reports the conflict. Resolve then requires the customer to select the winner and verifies that only it is recalled. | [memory #22](postmortems/memory.md#issue-22) | `pytest tests/test_conflict_detection.py tests/test_verified_remember.py tests/test_verified_resolve.py -q`; isolated browser acceptance covers unrelated direct save and Keep both → Resolve → Recall | 1x — complete Correct browser audit 2026-08-30; unrelated same-project live recurrence 2026-08-31 |
| BUG-068 | Recover staged and validated a real WAL-mode SQLite backup, then rejected the active copy with `MANIFEST_MISMATCH` and rolled back. The integrity check itself had created `-wal` and `-shm` files inside the staged tree after its manifest comparison; after that repair, a verified restore left Home showing the pre-restore snapshot until manual reload. | FIXED LOCALLY (guarded), unreleased — SQLite integrity uses an immutable read-only URI, and every data-tree verification compares the manifest both before and after database checks. A check that mutates the tree now fails during staging before the switch. Successful restore also clears stale memory selection and awaits one snapshot refresh before returning its receipt. | [database #13](postmortems/database.md#issue-13) + [dashboard #15](postmortems/dashboard.md#issue-15) | `pytest tests/test_verified_recovery.py tests/test_dashboard_ui.py -q`; isolated Home restore must immediately show the restored count, retain the baseline, remove the post-backup marker, and report `Restore verified` without page reload | 1x — real Home backup/restore audit 2026-08-30; stale post-restore Home recurrence 2026-08-31 |
| BUG-069 | A healthy direct visit to `http://localhost:8000` was labeled `Setup required` even when the snapshot already proved one active project. Full Home authority depended on an agent-issued secret URL fragment, and the dashboard process was not owned by the always-on daemon, so the customer entry point leaked transport mechanics and could disappear after restart. | FIXED, GUARDED, AND INSTALLED AS A LOCAL CANDIDATE; unreleased publicly — one daemon owns ports 8000 and 8765, direct Home discovers only the configured loopback daemon port, and the exact local origin can request one short-lived in-memory capability. The sole active project binds automatically; ambiguous project scope requires an explicit choice. Named operations retain origin, expiry, request-limit, confirmation, one-use-ticket, verification, and rollback gates. The service preserves certified-host discovery, and an unavailable daemon renders `Needs attention` while the snapshot remains readable. | [dashboard #15](postmortems/dashboard.md#issue-15) | `pytest tests/test_home_control.py tests/test_dashboard_ui.py tests/test_dashboard_serializer.py tests/test_mcp_daemon.py tests/test_developer_routing.py -q`; `pytest tests/test_install_setup.py tests/test_release_client_bundle.py -q`; dashboard production build; clean-source customer package verification; installed doctor; bare `http://localhost:8000` browser acceptance shows `Ready` without a fragment, connector, or agent command | 0x — first-time direct Home dogfood 2026-08-31 |
| BUG-070 | Home confused capability with proof; continuity checks exposed stale Review filters, hidden read errors, inaccessible related links, and misleading graph labels. | FIXED LOCALLY (guarded), candidate; public release separate — preserve the six-workspace repairs, exact stored edge rows and explicit reconnect without replay. Home clears stale search/detail on memory entry; header and Session Intelligence failures show unavailable state; related links are native buttons; graph type and safeguard-link labels follow actual data. | [dashboard #16](postmortems/dashboard.md#issue-16) | `pytest tests/test_dashboard_ui.py tests/test_dashboard_serializer.py tests/test_dashboard_snapshot_verifier.py tests/test_home_control.py tests/test_developer_routing.py -q`; production UI build and isolated browser operations with real readbacks | 5x — evidence/navigation/session, branching graph and continuous-workflow recurrences through 2026-09-03 |
| GAP-057 | The source now has receipt-producing A–F exact-package scenarios, but none has been executed against the final signed/notarized artifact. Native/accessibility states and three unfamiliar-user first-use trials also remain uncollected, and the product gate is not wired into tagged publication while distribution is deferred. | RUNNERS IMPLEMENTED LOCALLY / EVIDENCE EXECUTION OPEN — A/B/C/F install the supplied package into separate disposable roots before testing it; D uses a disposable root plus a distinct compatible baseline and forces a real stage-4 failure after payload switch; E independently proves backup, restore, uninstall, and reinstall. The gate reads the exact DMG, runs live macOS trust checks, hashes every private scenario receipt, verifies seven distinct native evidence files, and binds each of three human receipts to a real first-run receipt. Source tests, manifest claims, and the current tag workflow cannot pass as product evidence; unfamiliar-user status remains attested rather than machine-provable. | [Four-action PRD §11](proposals/four-action-product-lifecycle.md#11-product-release-scenarios) | `pytest tests/test_product_release_scenarios.py tests/test_product_release_gate.py tests/test_release_pipeline.py tests/test_mcp_daemon.py -q`; after separate release authorization, run A–F on clean certified lanes and call `scripts/ci/verify_product_release_gate.py --artifact ... --evidence-dir ...` with independently resolved artifact identity | 0x — product completion audit 2026-08-30 |
| GAP-053 | Task Intelligence can select and deliver a broadly relevant memory that does not contain the task-local decision evidence needed to improve the accepted result. Retrieval success can therefore look healthy while every agent repeats the same application failure. | OPEN, measured — task 032 delivered the intended installation-contract memory 3/3, but treatment accepted 0/3 and all five measured patches omitted the public Recall MCP tool. The next task must prove a discriminative memory fact absent from the source-only Brief before any model spend; do not patch task 032 or add more generic context. | [ai behavior #20](postmortems/ai-behavior.md#issue-21) | task 032 `memory-component` preflight; `summarize_task_intelligence_evaluation.py --brief-profile v2 --comparison memory-component --split calibration --task install-codex-recall-routing-black-box-032 --model gpt-5.6-sol --reasoning max --require-decision` | 1x — bounded causal component experiment 2026-08-13 |
| GAP-054 | The installed Codex workflow routed questions through Recall but did not tell the agent how to persist an explicit user request such as “remember this across sessions.” The live store therefore had no canonical mission or eligible decision-changing memory for the next Task Intelligence task. | CAPTURE FIXED in v2.13.0 (guarded), task lift OPEN — the reversible host guidance now requires explicit search-first capture, literal governance scope, future-question triggers, and a Recall delivery check. The live pilot stored the canonical mission; its first Recall correctly exposed an invalid prose scope, and Recall supplied the mission after scope was corrected to `elefante`. Ordinary conversation and secrets remain excluded. | [ai behavior #21](postmortems/ai-behavior.md#issue-22) | `pytest tests/test_task_intelligence.py -k "explicit_capture or governance_fails_closed" -q`; `pytest tests/test_install_setup.py -k "codex_recall_guidance or codex_memory_guidance" -q`; `pytest tests/test_mcp_daemon.py -k "client_protocol_excludes_developer_workspace_routing" -q`; live search→write→Recall proof with rollback ID `0b27fa62-d459-4029-a390-391305ab555d` | 1x — live memory-readiness audit and corrected delivery proof 2026-08-13 |
| GAP-055 | Task Intelligence treated correctness as the outcome and input-token growth as a secondary ceiling, so it did not operationalize the core objective of accepted task value per overall token, ignored output-token cost, and could omit unpaired early-stop spend from a comparison total. | FIXED in development (guarded), task lift OPEN — the report now separates complete-pair efficiency from all observed spend, counts total input plus output tokens, assigns failed outcomes zero value, rejects acceptance regression, reports accepted outcomes per million paired tokens, and requires a positive task-clustered lower bound before token intelligence can count as an effectiveness path. Consumed evidence remains diagnostic-only. | [ai behavior #22](postmortems/ai-behavior.md#issue-23) | `pytest tests/test_task_intelligence_report.py tests/test_task_intelligence_benchmark.py -q`; `python scripts/ci/verify_task_intelligence_benchmark.py`; replay task 032 with `--require-decision` and require `observed_total_tokens=1501308` | 0x — objective/evaluator contract audit 2026-08-14 |
| GAP-056 | The schema carried `trigger` and `surfaces_when` hints, but no runtime delivery path inspected explicit file, terminal-error, or conversation context, so opted-in proactive memory surfacing was not implemented. | FIXED in v2.13.0 (guarded) — typed file, terminal-error, and conversation envelopes now normalize every manifest host family, scrub secrets, enforce byte/field bounds, preserve provenance, and enter the loopback `/events/surface` read-only retrieval path without persistence. The governed selector still caps delivery at three and applies lifecycle, scope, source-trust, conflict, privacy, and current-source gates. Conservative semantic conflict detection and reversible Smart Merge/repair are also implemented; ambiguous conflicts require a user-selected winner and protected changes require explicit confirmation. | [ai behavior #23](postmortems/ai-behavior.md#issue-24) | `pytest tests/test_host_event_adapters.py tests/test_host_event_endpoint.py tests/test_proactive_surfacing.py tests/test_conflict_detection.py tests/test_conflict_resolution.py tests/test_mcp_daemon.py -q`; `git diff --check` | 0x — development closure 2026-08-28 |
| GAP-029 | The former runtime lock included ChromaDB with an unresolved advisory. | CLOSED (guarded) — fresh installations use SQLite, the production lock no longer includes ChromaDB, and the strict hash-locked audit reports no known vulnerabilities. Legacy migration utilities remain support tooling for existing stores and do not restore ChromaDB to the production dependency set. | [SQLite migration proposal](proposals/sqlite-vector-store-migration.md) | `uv tool run pip-audit --requirement requirements.lock --disable-pip --require-hashes --strict --progress-spinner off`; `rg -n -i "chromadb" requirements.txt requirements.lock` | 0x — closed by v2.12 release audit 2026-07-30 |
| BUG-001 | Kuzu SIGSEGV — QueryResult lifetime escapes GraphStore ownership | FIXED (guarded) | [ops-database #7](postmortems/database.md#issue-7) | `pytest tests/test_memory_persistence.py -k "graph_store_close or graph_store_raw_execute or live_mcp_server" -v` | 2x — fix now has 3 regression tests + runtime citation |
| BUG-002 | Kuzu database lock contention (multi-process) | FIXED (guarded) | [ops-database #2](postmortems/database.md#issue-2) | `pytest tests/test_memory_persistence.py -k "TestKuzuLockContract" -v` | 1x — guarded by fresh-path contract, cross-process citation, snapshot isolation, and active-doc sync tests |
| BUG-003 | Dashboard blank on first launch (race condition) | FIXED (guarded) | [ops-dashboard #8](postmortems/dashboard.md#issue-8) | `pytest tests/test_dashboard_serializer.py -k "dashboard" -v` | 1x — guarded by readiness wait, forced refresh restart, and frontend retry/backoff checks |
| BUG-004 | Dashboard scores stuck at 100 | FIXED | [ops-dashboard #9](postmortems/dashboard.md#issue-9) | `pytest tests/test_dashboard_serializer.py -v` | 1x |
| BUG-005 | Factory reset safety (destructive operation) | TESTED | [ops-database](postmortems/database.md) | `pytest tests/test_factory_reset.py -v` | 0x — 10 safety tests cover dry-run, gates, backup |
| BUG-006 | Agent entry point bypass — skips docs, guesses fix | FIXED (guarded) | [ops-ai-behavior #6](postmortems/ai-behavior.md#issue-6) | `.venv/bin/python scripts/verify/verify_e2e_tests.py` | 1x — guarded by first-response and first-error entrypoint injection |
| BUG-007 | Developer routing drift — stale versions, paths, storage claims, tool names, action inventories, proposal state, issue-ledger counts, and advertised-but-unimplemented or self-attested CLI checks in active guidance | FIXED AGAIN (guarded) — protocol headers derive v2.13.0; active proposal inventories, full Memory actions, unique issue IDs, derived BUG/GAP counts, and process-authored direct-restart PID/version enforcement have source-aligned regressions | [ops-ai-behavior #7](postmortems/ai-behavior.md#issue-7) | `./.venv/bin/python -m pytest tests/test_developer_routing.py -v` | 4x — broad documentation recurrence 2026-08-06; Gauntlet active-surface and self-attestation recurrences 2026-08-28 |
| BUG-008 | Graph/session schema drift lost relationship types and payloads, reversed incoming edges, reused nonexistent IDs, and accepted missing endpoints. | FIXED LOCALLY (guarded), candidate — all 18 named types and full payloads persist; additive legacy migration preserves old fields and unknown timestamps. Input preflight prevents ordinary invalid batches leaving orphan nodes; identical calls reuse actual IDs. | [ops-database #8](postmortems/database.md#issue-8) | `pytest tests/test_memory_persistence.py tests/test_mcp_daemon.py -q`; real old-schema migration, datetime properties, all-type readbacks and correction/recovery/snapshot guards | 2x — original schema guards; graph-authoring audit and repair 2026-09-03 |
| BUG-009 | Self-protocol verifier drift or live repeatability failure — stale snapshot-path assumptions, default line limits, a POSIX-only browser stub, legacy mutation aliases, missing strict project registration, and a cross-type preference merge have broken or overstated the maintained whole-system proof | FIXED AGAIN (guarded), unreleased — the 49/52 recurrence was one identity-alias cascade: when no preference candidate existed, preference reassertion fell back to any semantic match; random UUID tag fragments supplied the third shared keyword, so the active preference reused the graph decision ID. Deleting the active fixture then removed the graph anchor, leaving one real edge and only two later cleanup targets. Reassertion now considers actual preference records only, genuine preference reinforcement is preserved, and the harness fails immediately unless all four fixtures return distinct IDs. | [ops-ai-behavior #8](postmortems/ai-behavior.md#issue-8) | `./.venv/bin/python -m pytest tests/test_memory_persistence.py -q` (24 passed, 1 skipped); three consecutive `./.venv/bin/python scripts/verify/verify_e2e_tests.py` runs (52/52 each, 4/4 unique IDs, 5 graph rows, 3/3 cleanup, isolated store removed); keep `--with-dashboard-open` separate while installed port 8000 is active | 4x — original drift, portability recurrence 2026-08-28, strict-project/Correct recurrence 2026-08-30, non-repeatable identity alias 2026-09-01 |
| BUG-010 | Self-protocol cold-start deadlock — `from sentence_transformers import SentenceTransformer` hangs when executed in a worker thread under an active anyio event loop with piped stdio on Windows + Python 3.11 | FIXED (guarded) [WARN] Windows/Python 3.11/CPU only — untested on Linux, macOS, Python 3.12 | [ops-ai-behavior #9](postmortems/ai-behavior.md#issue-9) | `.venv/Scripts/python.exe scripts/verify/verify_e2e_tests.py` | 1x — guarded by pre-loading embedding model before event loop starts in server.py __main__ |
| BUG-011 | Memory add silently returned IGNORE — the test-memory guard gave no rejection reason and an overly broad heuristic blocked legitimate tags | FIXED (guarded) | [ops-memory #10](postmortems/memory.md#issue-10) | `pytest tests/test_memory_guard.py -v` | 2x — guarded by `rejection_reason` in `elefante-Memory(action="add")` responses |
| BUG-012 | Workspace-only instructions could leave a connected agent unaware of when to search persistent memory outside the Elefante repository. | FIXED (partial), released v2.12.2 — one account-level runtime is installed for detected compatible hosts; host instruction behavior still varies and unimplemented hosts remain Planned. | [ops-ai-behavior #10](postmortems/ai-behavior.md#issue-10) | In a detected configured host outside this repository, call `elefante-Memory` with `action="search"` and confirm the installed runtime responds | 1x — customer-global runtime removes per-checkout ownership; host certification remains separate |
| GAP-013 | JSON export is not a backup — `export_memories.py --format json` is a read-only analysis format with no import path. Embeddings are excluded (stored explicitly via `thenlper/gte-base`; import must regenerate them). Using ChromaDB default embedding on upsert would silently corrupt semantic search. | FIXED (guarded) — `import_memories.py` now validates the export, preserves IDs/metadata, regenerates embeddings with the configured local model, rejects collisions, requires `--confirm-stopped STOPPED` for apply plus a verified binary backup for non-empty targets, and rolls back partial writes. JSON remains a memory-migration format rather than a full backup: graph topology is not included; CSV remains analysis-only. | [ops-ai-behavior #11](postmortems/ai-behavior.md#issue-11) | `pytest tests/test_import_memories.py -v`; full recovery remains `pytest tests/test_backup_restore.py -v` | 0x — first discovery; additive importer guarded 2026-08-27 |
| BUG-014 | CI binary build failure — `build-binaries.yml` had no Node.js/npm step so `src/dashboard/ui/dist/` (gitignored) did not exist in CI. Additionally `elefante.spec` referenced `src/dashboard/ui/build` but Vite outputs to `dist`. All three matrix jobs failed on first tag push (v2.5.3). | FIXED — v2.5.4 | [ops-installation #8](postmortems/installation.md#issue-8) | `Select-String -Path elefante.spec -Pattern "dashboard/ui"` → must show `dist`; `Select-String -Path .github/workflows/build-binaries.yml -Pattern "setup-node\|npm ci"` → must show both | 0x — first tag push |
| BUG-015 | GitHub Release publish failure — the `v2.6.0` release object was created and macOS/Windows assets uploaded, but the Linux artifact was `4,021,041,080` bytes. GitHub release assets must be under `2 GiB`, so `Create GitHub Release` failed while attaching the Linux zip. | FIXED — v2.7.1 (confirmed: v2.7.1, v2.6.0, v2.5.4 all published with 4 assets on GitHub Releases) | [ops-installation #9](postmortems/installation.md#issue-9) | `pytest tests/test_release_pipeline.py -v` | 1x — v2.6.0 publish failure |
| BUG-016 | Domain signal value-space disjunction — `analyze_query()` infers `None`/`"work"`/`"personal"`/`"project:elefante"` but memories default to `DomainType.REFERENCE`. The value spaces never intersect. 15% of composite weight produces 0.5 (neutral) or 0.0 (penalty), never 1.0. Domain actively degrades ranking vs pure vector. | FIXED — v2.7.0 | [ops-memory #11](postmortems/memory.md#issue-11) | `pytest tests/test_autonomous_coactivation.py -v` | 0x — first discovery via ARAA analysis |
| BUG-017 | Unconditional spec override dominates all queries — `+0.30` boost applied to all specification/directive memories regardless of query intent. Three different real queries all returned the same top 4 specification memories. Non-spec memories mathematically cannot outrank specs. | FIXED — v2.7.0 | [ops-memory #12](postmortems/memory.md#issue-12) | `pytest tests/test_autonomous_coactivation.py -v` | 0x — first discovery via ARAA analysis |
| BUG-018 | Historical co-activation cold-start — the former `_session_retrieval_history` reset on server restart, so retrieved IDs were unavailable to the read path. | SUPERSEDED by BUG-048 — ordinary retrieval no longer creates co-activation, and Task Intelligence declared-use stays in a separate observational ledger. Historical v2.7 repair remains preserved in the postmortem. | [ops-memory #13](postmortems/memory.md#issue-13); [ai behavior #16](postmortems/ai-behavior.md#issue-16) | `pytest tests/test_autonomous_coactivation.py -k "exposure or coactivation" -v` | Historical recurrence closed; contract replaced 2026-08-08 |
| BUG-019 | DMG GUI installer .app broken — `installer_gui.py` corrupted by overlapping multi-edit patches (dark-to-light-mode rewrite). Fatal `SyntaxError` at line 173, undefined variables (`style`, bare `C`), duplicate widget constructors, nonexistent palette keys. .app exits code 1, no GUI window appears. File was never committed so no git recovery possible. | FIXED — v2.8.1 | [ops-installation #10](postmortems/installation.md#issue-10) | `python3 -c "import py_compile; py_compile.compile('scripts/ci/installer_gui.py', doraise=True)"` + DMG launch test | 0x — first discovery, regression from same session |
| BUG-020 | DMG installer customer surface broken — the `.app` launches and internal controls exist, but the rendered macOS window remains visually broken and unacceptable as a customer installer experience. The root fix is to replace Tk as the primary macOS surface with native AppKit and verify by screenshot, not just process liveness. | FIXED (guarded) — v2.9.0 | [ops-installation #11](postmortems/installation.md#issue-11) | `pytest tests/test_installer_gui.py -v` + `swiftc -parse-as-library -O scripts/ci/installer_app.swift -o /tmp/elefante-installer-native-test`; then Screenshot first → native compile → install smoke; widget-tree inspection only if screenshot fails (manual packaged-artifact screenshot remains authoritative) | 0x — first customer-experience audit |
| BUG-021 | Installer seed-memory collision with test-memory guard — every fresh install failed at stage 3 (Database Initialization) because `init_databases.py::inject_seed_memory` submitted `tags=["seed", "test", "passcode"]` and the orchestrator guard (BUG-011) does exact-match on tag `"test"` to block E2E artifacts. The guard rejected the installer's own seed, the install reported FAILED, and no regression test caught it because no pytest exercised the positive (pass) path. | FIXED (guarded) — v2.9.1 | [ops-installation #12](postmortems/installation.md#issue-12) | `pytest tests/test_install_setup.py -v` (guards the seed payload against every guard condition) | 1x — first full customer-experience audit |
| BUG-023 | Installer reuse mode assumed every valid `.venv` already had `pip`. A fresh `uv`-created environment reached Step 2 with a working Python 3.11 interpreter but failed immediately on `python -m pip` with `No module named pip`. | FIXED (guarded), released by v2.12.2 | [ops-installation #13](postmortems/installation.md#issue-13) | `./.venv/bin/python -m pytest tests/test_install_setup.py -k "bootstraps_pip_when_missing" -v` | 1x — first fresh-environment audit after Homebrew Python removal |
| BUG-024 | Installer bundle build walks local `.venv.*` backups — `build_installer_bundle.py` excluded only exact `.venv`, so a recovered workspace with `.venv.broken.<timestamp>` tried to package broken interpreter symlinks, crashed with `FileNotFoundError`, and left stale `dist` artifacts behind. | FIXED (guarded), released by v2.12.2 | [ops-installation #14](postmortems/installation.md#issue-14) | `./.venv/bin/python -m pytest tests/test_installer_bundle.py -k "venv_backups" -v` | 1x — first post-recovery bundle rebuild |
| BUG-022 | Legacy ChromaDB `query()` with `where` failed on a production collection and blocked preference writes. Fresh v2.12.2 installations use SQLite. | FIXED (guarded) — v2.9.0 legacy path; SQLite default since v2.11.1 | [ops-memory #14](postmortems/memory.md#issue-14) | On an explicitly configured legacy fixture, run `elefante-Memory` with `action="add"` and `memory_type="preference"`; confirm `success: true` | 0x — first discovery |
| GAP-025 | Multi-instance write origin tracking was absent, and stdio-per-client transport made two concurrent IDEs a Kuzu single-writer risk. | CLOSED for this installed customer store on 2026-08-28 — the released daemon, Source tuples, adapters, and two-client proof protect fresh installs; the explicitly authorized existing-user migration was applied while the daemon was stopped after a verified backup. It added five Memory entities and five source links, then a repeat dry-run reported zero pending work and final doctor returned customer-ready. Other installations still require their own backup-bound migration. | [ops-memory #15](postmortems/memory.md#issue-15) | `pytest tests/test_memory_persistence.py -k "backfill or provenance" -q`; verified backup receipt in the operator report; repeat dry-run and customer doctor | 0x — local installed migration completed 2026-08-28 |
| BUG-026 | DOC_SYNC protocol bypass — direct-repo file-edit agent skips Loop Step 1 (Known Issues) at the moment of action, applies Gate 3 (Leakage Scan) after authoring redundant content instead of before, performs unauthorized private-memory side writes, and self-evaluates falsely. Recurrence of BUG-006 in a new surface (file-edit) where the shipped MCP-response `ENTRYPOINT_SEQUENCE` injection cannot fire. Parent class: BUG-006. | MITIGATED (guarded) — active guard on filename-pattern subset lands 2026-05-02 in `tests/test_developer_routing.py::test_no_forbidden_filename_patterns_in_active_docs_or_agents`. Broader passive-protocol failure (Loop Step 1 skip, Gate 3 timing, side writes) remains passive-only. | [postmortems/ai-behavior.md Issue #12](postmortems/ai-behavior.md) | `pytest tests/test_developer_routing.py::test_no_forbidden_filename_patterns_in_active_docs_or_agents -v` (filename guard); for non-filename failure modes, manual route per `agents/orchestrator.md` Documentation Skill | 3x — discovery 2026-05-02 + 3 same-session recurrences (filename version-stamping; audit-without-reread; deferred-deletion-of-forbidden-pattern-files); guard added same day |
| BUG-027 | File-edit destructive op without preservation — an agent can mutate uncommitted files without a pre-edit status check or recoverable copy. | MITIGATED (passive only) — the development lifecycle requires `git status --porcelain <path>` plus commit-or-archive-first. No active filesystem hook enforces this yet; that work is Upcoming without a version promise. | [postmortems/ai-behavior.md Issue #12](postmortems/ai-behavior.md) (parent BUG-006) + [proposals/_archive/surface-split-full.md](proposals/_archive/surface-split-full.md) + [proposals/_archive/documentation-strategy-protocol-full.md](proposals/_archive/documentation-strategy-protocol-full.md) | Before destructive file work, run `git status --porcelain <path>`; preserve untracked or modified content before proceeding | 1x — discovery 2026-05-02 (1033 LOC lost) |
| GAP-028 | Hermes needed a configured model provider before it could exercise Elefante from the consumer side. | CLOSED on 2026-05-02 — a historical end-to-end run called `elefante-Memory` search behavior and returned the expected stored evidence. Current Hermes provider and runtime state are **UNKNOWN** until reverified; no temporary path or credential state is a durable contract. | none — historical environment capability | Reverify with the current Hermes release and compare Elefante's surface against `./.venv/bin/python scripts/ci/list_mcp_tools.py` | 0x — closed on first audit |

- **FIXED (guarded)**: Fix is in place AND has regression tests. If the test still passes, the fix holds. If it fails, the regression is real.
- **FIXED (documented)**: Fix is in place, recovery procedure documented, but no automated regression guard yet.
- **TESTED**: Feature works and has test coverage. No known bug, but the test exists because the risk is high.
- **OPEN**: Known weakness, mitigation in place, but not fully resolved.

### Adding a New Issue

1. Assign next `BUG-NNN` ID
2. Document the postmortem in the relevant `postmortems/<domain>.md`
3. Write or identify the test that proves the fix
4. Add the row to this table
5. If the error surfaces in Python, add a runtime citation pointing to the postmortem entry (see [`../agents/orchestrator.md`](../agents/orchestrator.md) Embedding Rule)

## Layout

```
workspace/
├── ISSUES.md                   <- This file: BUG/GAP tracker (you are here)
├── lessons.md                  <- Cross-bug rules (Promotion-Filter qualified)
├── postmortems/                <- Domain compendiums (one file per domain)
│   ├── ai-behavior.md
│   ├── dashboard.md
│   ├── database.md
│   ├── installation.md
│   ├── memory.md
│   └── _archive/               <- Pre-distillation full narratives
└── PLANNING.md                 <- Active release state + §10 Journal
```

Whole-system MCP verification spec lives in [`../docs/reference/self-protocol.md`](../docs/reference/self-protocol.md) (positive contract, not a postmortem).

Repository debugging uses existing maintained verification first:

- **[`../agents/orchestrator.md`](../agents/orchestrator.md)** is the operational authority that routes script choices
- **[`../scripts/verify/`](../scripts/verify/)** for purposeful validation selected by the orchestrator
- **[`../tests/README.md`](../tests/README.md)** for targeted pytest coverage preferred over scratch repro scripts
- **[`../scripts/debug/`](../scripts/debug/)** for last-resort interventions only when a postmortem calls for them

---

## Domain Postmortems

Each postmortem is distilled to atomic Trigger / Root cause / Solution / Lesson chunks. Full pre-distillation narrative in [`postmortems/_archive/<domain>-full.md`](postmortems/_archive/).

| Domain       | Postmortem                                       |
| ------------ | ------------------------------------------------ |
| Dashboard    | [postmortems/dashboard.md](postmortems/dashboard.md)       |
| Database     | [postmortems/database.md](postmortems/database.md)         |
| Installation | [postmortems/installation.md](postmortems/installation.md) |
| Memory       | [postmortems/memory.md](postmortems/memory.md)             |
| AI Behavior  | [postmortems/ai-behavior.md](postmortems/ai-behavior.md)   |

### Orchestrator (Operational Authority)

[`../agents/orchestrator.md`](../agents/orchestrator.md) — single operational authority for AI agents developing Elefante.

### Feedback Loop Ledger

[`lessons.md`](lessons.md) — distilled cross-bug rules that connect issue rows, postmortems, maintained tests, and the live workflow.

### Whole-System Verification

[`../docs/reference/self-protocol.md`](../docs/reference/self-protocol.md) — authoritative whole-system proof for the live MCP server.

---

## How to Use

| Task            | Action                                                                        |
| --------------- | ----------------------------------------------------------------------------- |
| **First step**  | Check the Known Issues table above — if the error matches, run the verification command |
| **Deep dive**   | Open the linked domain postmortem, then run the issue row's verification command |
| **Validate**    | Use [`../agents/orchestrator.md`](../agents/orchestrator.md) plus [`../tests/README.md`](../tests/README.md) to choose the smallest existing verifier |
| **Whole-system proof** | Run `./.venv/bin/python scripts/verify/verify_e2e_tests.py` and use [`../docs/reference/self-protocol.md`](../docs/reference/self-protocol.md) to interpret coverage and exclusions |
| **Intervene**   | Use `scripts/debug/` only when the compendium says verification is insufficient |
| **New issue**   | Assign next BUG-NNN → post-mortem in compendium → test → add row to Known Issues |

---

_Verification evidence belongs to each issue row and the exact journal entry in `PLANNING.md`. This tracker makes no repository-wide current pass-count claim without a fresh run._
