services:
  elefante:
    build: .
    container_name: elefante
    environment:
      ELEFANTE_DATA_DIR: /data
      ELEFANTE_DASHBOARD_HOST: 0.0.0.0
      PYTHONUNBUFFERED: "1"
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./elefante_data:/data
    restart: unless-stopped
